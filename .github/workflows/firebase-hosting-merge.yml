# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Install
        run: yarn install
      - name: Run lint check
        run: yarn lint
      - name: Test
        run: yarn test
  build_and_deploy_stage:
    runs-on: ubuntu-latest
    needs: tests
    steps:
      - uses: actions/checkout@v2
      - name: Build Stage
        run: yarn build
        env:
          REACT_APP_BACKEND_URL: ${{ secrets.REACT_APP_BACKEND_STATINA_URL_STAGE }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_STATINA_STAGE }}'
          channelId: live
          projectId: statina-stage
  build_and_deploy_prod:
    needs: build_and_deploy_stage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build Stage
        run: yarn build
        env:
          REACT_APP_BACKEND_URL: ${{ secrets.REACT_APP_BACKEND_STATINA_URL_PROD }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_STATINA }}'
          channelId: live
          projectId: statina-83aa9
